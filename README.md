# 庭院 IoT App

庭院 IoT 控制应用的产品文档与可交互移动端原型。

## 在线页面

本项目通过 GitHub Actions 自动发布到 GitHub Pages。每次向 `main` 分支推送代码后，线上页面会自动更新。

## 本地开发

```bash
cd prototype
npm ci
npm run dev
```

## 构建

```bash
cd prototype
npm run build
```
